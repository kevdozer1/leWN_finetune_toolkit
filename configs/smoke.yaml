# End-to-end smoke test config.
# Uses the local pilot HDF5 and the local lewm-cube pretrained checkpoint.
# Intentionally tiny: 1 epoch, batch 4, so it finishes fast on any GPU/CPU.

pretrained_path: D:/hf_cache/models--quentinll--lewm-cube/snapshots/7d05e023b3c1114cc8e803ec23fb0177d688598b
dataset_name: pilot_boring3d
data_cache_dir: D:/bridgedata_v2_subset

action_dim: 7
frameskip: 1
history_size: 3
num_preds: 1
batch_size: 4
num_workers: 0
max_epochs: 1
lr: 5.0e-5
sigreg_weight: 0.09
precision: bf16-mixed
seed: 42
run_name: smoke
